"""CLI entrypoints for the spike: tournament-evaluator workflow.

These scripts are operator-run (require `.env` with API_FOOTBALL_KEY).
The tested infrastructure lives in src/bip/evaluation/; these files wire
it together.
"""
