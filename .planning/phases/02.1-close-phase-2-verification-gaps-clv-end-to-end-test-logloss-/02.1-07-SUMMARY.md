---
phase: 02.1-close-phase-2-verification-gaps-clv-end-to-end-test-logloss-
plan: 07
subsystem: api
tags: [odds-api, pinnacle, stub, plumbing, clv, structlog]

# Dependency graph
requires:
  - phase: 02.1
    provides: OddsApiClient (existing) — fetch_pinnacle_closing_odds, retry/backoff scaffolding, structlog logger
provides:
  - OddsApiClient.fetch_historical_closing(event_id, sport="soccer_epl") -> dict | None plumbing stub
  - Call-site signature contract for plan 02.1-10 pipeline CLV wire to import safely
  - Structlog warning event "pinnacle_historical_not_implemented" for observability when stub is hit
affects: [02.1-10, 02.1-11, post-02.1-pinnacle-backfill]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Plumbing stub returns None + logger.warning rather than NotImplementedError, so callers handle gracefully without try/except boilerplate"

key-files:
  created: []
  modified:
    - src/bip/clv/client.py

key-decisions:
  - "Phase 02.1 P07 — fetch_historical_closing stub returns None (not raises) per D-01: callers in 02.1-10 pipeline can call directly without try/except; warning emitted via structlog for observability"
  - "Phase 02.1 P07 — no @retry decorator on stub: retry stack belongs with the real /v4/historical implementation, not a no-op"

patterns-established:
  - "Pattern: deferred-implementation stubs in BIP use logger.warning + return None (not NotImplementedError) so the dual-source plumbing can land before the second source is wired"

requirements-completed: [ML-02]

# Metrics
duration: 1min
completed: 2026-05-02
---

# Phase 02.1 Plan 07: OddsApiClient.fetch_historical_closing Stub Summary

**Plumbing stub for Pinnacle historical /v4/historical endpoint — returns None + structlog warning per D-01 so plan 02.1-10 can wire the dual-source CLV path without crashing.**

## Performance

- **Duration:** 1min
- **Started:** 2026-05-02T04:29:59Z
- **Completed:** 2026-05-02T04:31:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added `fetch_historical_closing(event_id: str, sport: str = "soccer_epl") -> dict | None` as last method on `OddsApiClient`
- Method body emits `logger.warning("pinnacle_historical_not_implemented", event_id=..., sport=..., note="...")` then returns `None`
- No `@retry` decorator (deliberate — retry stack lands with real implementation post-02.1)
- Docstring documents the stub status and target endpoint `/v4/historical/sports/{sport}/events/{event_id}/odds`
- All 117 existing tests still pass (1 skipped is the pre-existing Wave-0 mark)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add fetch_historical_closing stub to OddsApiClient** — `b537709` (feat)

## Files Created/Modified
- `src/bip/clv/client.py` — Appended 22 LOC: `fetch_historical_closing` async stub method with docstring + structlog warning + `return None`

## Decisions Made
- Stub body chose `logger.warning + return None` over `raise NotImplementedError` per D-01, so plan 02.1-10 callers do not need to wrap calls in try/except.
- Omitted the `@retry` decorator on the stub: retry/backoff is a network-layer concern that belongs with the real HTTP call, not the no-op.
- Method placed at the end of the class (after `map_market_key`) to mirror the natural reading order: HTTP fetch methods first, helpers, then deferred-stubs.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## Known Stubs

| Stub | File | Line | Reason |
|------|------|------|--------|
| `fetch_historical_closing` returns `None` unconditionally with `logger.warning` | src/bip/clv/client.py | 115-135 | Intentional plumbing stub per D-01. Real implementation against The Odds API `/v4/historical/sports/{sport}/events/{event_id}/odds` is deferred post-02.1 once API-Football CLV numbers are validated. Plan 02.1-10 will exercise the None-handling path synthetically. |

This stub is the explicit deliverable of this plan, not an unplanned shortcut. The Phase 02.1 verifier should treat the `return None` body as in-scope.

## Threat Surface Scan

No new network endpoints, auth paths, or trust boundaries introduced — stub does not call out, does not read secrets, and the only logged values are `event_id` (opaque ID) and `sport` (enum string). The Odds API key (`self._api_key`) is set in `__init__` and used only in `fetch_pinnacle_closing_odds` request params — verified by `grep -n "ODDS_API_KEY\|api_key\|apikey\|API_KEY" src/bip/clv/client.py` showing matches only at lines 42-43 and 88, none inside the new method body.

## User Setup Required

None — no external service configuration required. The Odds API key handling is unchanged from prior plans.

## Next Phase Readiness
- Plan 02.1-10 (pipeline CLV wire) can now import `OddsApiClient.fetch_historical_closing` and call it; the `None` return path is the documented contract.
- Real `/v4/historical` implementation deferred to a post-02.1 follow-up task once API-Football CLV numbers prove the dual-source architecture is worth completing.

## Self-Check: PASSED

- File `src/bip/clv/client.py` exists and contains `def fetch_historical_closing` at line 115 (verified via grep)
- Commit `b537709` exists in git log (verified below)
- All acceptance criteria grep checks return ≥1 match
- `uv run pytest tests/ -q` exits 0 (117 passed, 1 skipped)

---
*Phase: 02.1-close-phase-2-verification-gaps-clv-end-to-end-test-logloss-*
*Completed: 2026-05-02*
