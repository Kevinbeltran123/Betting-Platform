---
phase: 02.1
slug: close-phase-2-verification-gaps-clv-end-to-end-test-logloss
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-24
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x + pytest-asyncio 0.25.x |
| **Config file** | `pyproject.toml` ([tool.pytest.ini_options]) |
| **Quick run command** | `uv run pytest tests/ -x --no-header -q` |
| **Full suite command** | `uv run pytest tests/ -v` |
| **Estimated runtime** | ~30-60 seconds (synthetic; real smoke train is one-shot exit check, not sampled) |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/ -x --no-header -q` (fail-fast, first-failure-stops)
- **After every plan wave:** Run `uv run pytest tests/ -v` (full suite, verbose)
- **Before `/gsd-verify-work`:** Full suite must be green + real PL smoke train exit check (one-shot)
- **Max feedback latency:** 60 seconds for sampled suite

---

## Per-Task Verification Map

*To be populated by the planner during PLAN.md generation. Every `<task>` should have an `<automated>` block referencing one of the commands below, or a `Wave 0` stub dependency.*

| Task ID | Plan | Wave | Requirement | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------------|-----------|-------------------|-------------|--------|
| TBD — planner fills | — | — | — | — | — | — | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Test-file stubs that MUST exist before the plans in later waves can be verified. The planner's Wave 0 plan(s) must create these (empty skeleton with xfail or pytest.skip markers is fine — the point is to have the file path reachable by downstream `<automated>` commands).*

- [ ] `tests/test_training_pipeline_e2e.py` — synthetic end-to-end pipeline test (D-17 synthetic gate)
- [ ] `tests/test_backtest_simulate_pick.py` — simulate_pick() + EDGE_THRESHOLD_PCT unit tests (D-09, D-10)
- [ ] `tests/test_metadata_logloss.py` — ModelMetadata logloss-field serialization round-trip (D-13)
- [ ] `tests/test_parquet_store_results_odds.py` — read_results / read_odds / write_results / write_odds (D-04)
- [ ] `tests/test_apifootball_get_odds.py` — ApiFootballClient.get_odds() mocked response (D-07, adjusted for RESEARCH 7-day lookback finding)
- [ ] `scripts/verify_migration_003.py` — runnable Python script that queries information_schema (D-15 exit check)

*If the planner finds that existing `tests/` infrastructure covers one of the above, the corresponding checkbox can be dropped with a note in the plan.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Migration 003 applied to live Supabase | D-15 exit criterion | Requires live DB credentials + destructive DDL | 1. Ensure `.env` has `SUPABASE_DB_URL` / `SUPABASE_DB_PASSWORD`. 2. Run `supabase link --project-ref <ref>` (one-time). 3. `supabase db push`. 4. `uv run python scripts/verify_migration_003.py` — asserts column + index exist. |
| Real Premier League smoke train | D-16 exit criterion | Takes ~10-15 min wall time + API-Football Pro credits | 1. `uv run python scripts/seed_historical.py --seasons 2024-2025 --leagues premier_league`. 2. `uv run python -m bip.train fit --league premier_league --version v1`. 3. Open `models/football/premier_league/v1/metadata.json`, assert: `walk_forward_mean_clv_pct` is non-zero float, ≥3/5 folds have non-None `clv_pct`, `logloss_improvement_pct > 0`. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (stubs for test files listed above)
- [ ] No watch-mode flags (pytest --looponfail, pytest-watch, jest --watch, etc.)
- [ ] Feedback latency < 60s for sampled quick-run
- [ ] `nyquist_compliant: true` set in frontmatter (only after planner maps every task to an automated verify)

**Approval:** pending
