---
phase: 02.1-close-phase-2-verification-gaps-clv-end-to-end-test-logloss-
plan: 00
subsystem: testing
tags: [pytest, stubs, wave-0, scaffolding, nyquist]

# Dependency graph
requires:
  - phase: 02
    provides: existing pytest infrastructure (conftest.py fixtures, pytest-asyncio, pytest-httpx)
provides:
  - tests/test_training_pipeline_e2e.py — D-17 synthetic e2e test skeleton (consumed by plan 02.1-11)
  - tests/test_backtest_simulate_pick.py — D-09 / D-10 simulate_pick unit test skeleton (consumed by plan 02.1-03)
  - tests/test_metadata_logloss.py — D-13 ModelMetadata logloss-field round-trip skeleton (consumed by plan 02.1-04)
  - tests/test_parquet_store_results_odds.py — D-04 results/odds round-trip skeleton (consumed by plan 02.1-05)
  - tests/test_apifootball_get_odds.py — D-07 get_odds dispatch skeleton (consumed by plan 02.1-06)
  - scripts/verify_migration_003.py — D-15 migration verification entry-point skeleton (consumed by plan 02.1-01)
affects: [02.1-01, 02.1-03, 02.1-04, 02.1-05, 02.1-06, 02.1-11]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Module-level pytest.mark.skip for Wave 0 stubs ("Phase 02.1 Wave 0 stub — plan 02.1-NN implements ...")
    - Script skeleton exits 1 when required env var missing; never claims success in stub form
    - Stub-marker grep convention: every Wave 0 stub file contains the substring "Phase 02.1 Wave 0 stub"

key-files:
  created:
    - tests/test_training_pipeline_e2e.py
    - tests/test_backtest_simulate_pick.py
    - tests/test_metadata_logloss.py
    - tests/test_parquet_store_results_odds.py
    - tests/test_apifootball_get_odds.py
    - scripts/verify_migration_003.py
  modified: []

key-decisions:
  - "Use module-level pytestmark.skip (not xfail) so stubs are visible in --collect-only without showing as failed"
  - "verify_migration_003.py stub returns 1 even when SUPABASE_DB_PASSWORD is set — plan 02.1-01 fills the real body; stub never claims success"
  - "Created dedicated new files (test_backtest_simulate_pick.py, test_parquet_store_results_odds.py, test_apifootball_get_odds.py) rather than extending existing Phase 2 tests, to keep each downstream plan's diff surface minimal and avoid accidental edits"

patterns-established:
  - "Wave 0 stubs use a single docstring line plus pytestmark.skip so the file is collectable but no test runs until the consuming plan replaces the marker"
  - "Stub script skeleton: exit code 1 by default, single check for required env var, prints STUB: marker so manual runs cannot mistakenly mark Phase 02.1 D-15 complete"

requirements-completed: [ML-02, ML-03]

# Metrics
duration: 4min
completed: 2026-05-02
---

# Phase 02.1 Plan 00: Wave 0 Stubs Summary

**Created 6 Wave 0 file skeletons (5 pytest stubs + 1 ops script) so every downstream Phase 02.1 plan's `<automated>` command resolves a real file path; pytest --collect-only succeeds and the existing 95-test suite remains green.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-05-02T03:42:30Z
- **Completed:** 2026-05-02T03:46:17Z
- **Tasks:** 1
- **Files modified:** 6 (all created)

## Accomplishments

- Every Wave 1+ plan in Phase 02.1 can now run its `<automated>` block without a `pytest: file not found` error
- 14 stub tests collected by pytest (5 files, all skipped at module level)
- `scripts/verify_migration_003.py` exits 1 without `SUPABASE_DB_PASSWORD` (correct stub behavior — plan 02.1-01 will implement the real psycopg query body)
- Existing 95-test Phase 2 suite still passes (no regressions)

## Task Commits

Each task was committed atomically:

1. **Task 0: Create all five pytest stubs + verify_migration_003.py skeleton** — `63f479d` (test)

## Files Created/Modified

- `tests/test_training_pipeline_e2e.py` — D-17 synthetic e2e test skeleton; module skip + 1 placeholder
- `tests/test_backtest_simulate_pick.py` — D-09 / D-10 stubs; module skip + 4 placeholders
- `tests/test_metadata_logloss.py` — D-13 stub; module skip + 2 placeholders
- `tests/test_parquet_store_results_odds.py` — D-04 stubs; module skip + 4 placeholders
- `tests/test_apifootball_get_odds.py` — D-07 dispatch stubs; module skip + 3 async placeholders
- `scripts/verify_migration_003.py` — D-15 ops-script skeleton; exits 1 when `SUPABASE_DB_PASSWORD` unset, returns 1 with STUB marker otherwise

## Decisions Made

- **Module-level skip vs xfail:** chose `pytestmark = pytest.mark.skip(reason=...)` so collect-only succeeds and downstream plans can incrementally remove the marker. xfail would conflate stub-state with expected-failure semantics.
- **Stub script returns 1 even when env var is set:** prevents an accidental `verify_migration_003.py` invocation from being interpreted as D-15 passing. Plan 02.1-01 lands the real body and flips the success path to return 0.
- **Three brand-new test files (not extensions):** the planner's PATTERNS.md proposes extending `tests/test_backtest_clv.py`, `tests/test_parquet_store.py`, and `tests/test_api_football_client.py` for the production wiring tasks. To avoid touching Phase 2 tests during Wave 0 scaffolding (and to keep each downstream plan's diff small), Wave 0 creates dedicated new files. Plans 02.1-03/05/06 may consolidate into the existing files or keep these — that decision lands with each consuming plan.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Wave 0 unblocks plans 02.1-01 through 02.1-12. Every `<automated>` block in those plans now references an existing file path.
- Plan 02.1-01 (Wave 1) can now fill in `scripts/verify_migration_003.py` with the real psycopg query body for D-15.
- Plans 02.1-03, 02.1-04, 02.1-05, 02.1-06, 02.1-11 each replace one stub file's `pytestmark.skip` with real assertions.

## Self-Check: PASSED

- FOUND: tests/test_training_pipeline_e2e.py
- FOUND: tests/test_backtest_simulate_pick.py
- FOUND: tests/test_metadata_logloss.py
- FOUND: tests/test_parquet_store_results_odds.py
- FOUND: tests/test_apifootball_get_odds.py
- FOUND: scripts/verify_migration_003.py
- FOUND commit: 63f479d
- VERIFIED: `uv run pytest tests/.../--collect-only -q` collected 14 tests (exit 0)
- VERIFIED: `SUPABASE_DB_PASSWORD='' uv run python scripts/verify_migration_003.py` → exit 1
- VERIFIED: `uv run pytest tests/ -q` → 95 passed, 14 skipped, 0 failed (no regressions)

---
*Phase: 02.1-close-phase-2-verification-gaps-clv-end-to-end-test-logloss-*
*Completed: 2026-05-02*
