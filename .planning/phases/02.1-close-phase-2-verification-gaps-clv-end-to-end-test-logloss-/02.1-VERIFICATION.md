---
phase: 02.1-close-phase-2-verification-gaps-clv-end-to-end-test-logloss-
verified: 2026-04-30T00:00:00Z
status: human_needed
score: 11/11 must-haves structurally verified; 2 documented deferrals require live-API empirical confirmation
overrides_applied: 0
re_verification: # First verification of phase 02.1 (closes Phase 2 gaps)
  previous_status: gaps_found
  previous_phase: "02-ml-core-football"
  gaps_closed:
    - "Walk-forward backtest produces per-fold CLV estimates using opening odds with 1-2% slippage (Phase 2 Truth 1)"
    - "Calibration logloss improvement is documented per league (Phase 2 Truth 2)"
  gaps_remaining: []
  regressions: []
deferred:
  - truth: "Empirical confirmation of non-zero walk_forward_mean_clv_pct on a real Premier League smoke train (D-16)"
    addressed_in: "Phase 3 prep"
    evidence: "Plan 02.1-12 status=deferred-verification (commit 43f7544); user accepted risk of spending ~1,520 API-Football Pro credits during Phase 02.1 execution. Code path is structurally complete (Plan 02.1-10 commit 07b20a6) and synthetic gate passes (Plan 02.1-11 commit 012fb31)."
  - truth: "Empirical confirmation that bulk /odds?league&season returns historical Betano coverage (Research A1+A2)"
    addressed_in: "Phase 3 prep"
    evidence: "Plan 02.1-02 status=completed-with-deferred-verification (commit 11806af). Probe script committed at scripts/probe_odds_bulk.py but never executed against the live API; user accepted risk to avoid spending an API-Football credit on reconnaissance before the seed extension lands."
human_verification:
  - test: "Run scripts/verify_migration_003.py against live Supabase with SUPABASE_DB_PASSWORD set"
    expected: "Script prints 'OK: migration 003 applied — is_shadow column + index present' and exits 0"
    why_human: "Requires SUPABASE_DB_PASSWORD in local .env. The equivalent SQL queries (information_schema.columns + pg_indexes) were verified via Supabase MCP during Plan 02.1-01 — the column + index exist in live project hpcbctvcmcijwjvzecvx — but the script-level evidence was never captured because the session never had the DB password populated. D-15 is structurally satisfied; this is a script-evidence checkbox."
  - test: "Run scripts/probe_odds_bulk.py --league-id 39 --season 2024 against live API-Football"
    expected: "Probe writes 02.1-ODDS-PROBE.md with results_count > 0, betano_present_in_sample=True, exits 0"
    why_human: "Costs one API-Football Pro request. User deferred per Plan 02.1-02 risk-acceptance. Required before Plan 02.1-12 smoke train can reliably produce coverage."
  - test: "Run real Premier League 2024-2025 smoke train end-to-end (D-16 exit gate)"
    expected: "1) `uv run python scripts/seed_historical.py --seasons 2024-2025 --leagues premier_league` completes; 2) `uv run python -m bip.train fit --league premier_league --version v1` produces models/football/premier_league/v1/metadata.json with: walk_forward_mean_clv_pct as a non-zero float, ≥3 of 5 walk_forward_fold_details have non-None clv_pct, logloss_improvement_pct > 0"
    why_human: "Costs ~1,520 API-Football Pro credits and ~10-15 min wall time. User explicitly deferred per Plan 02.1-12 risk-acceptance (commit 43f7544). The CODE PATH is structurally complete and proven on synthetic data (Plan 02.1-11), but ML-02 / ML-03 only flip from 'structurally PASS' to 'empirically PASS' once real numbers land in metadata.json."
---

# Phase 02.1: Close Phase 2 Verification Gaps (CLV End-to-End + Logloss) — Verification Report

**Phase Goal:** Wire `apply_slippage` + `compute_clv` into `TrainingPipeline.run()`, extend `seed_historical.py` to fetch historical opening odds (Betano via API-Football) + match results, surface real per-fold CLV numbers in `metadata.json`, and persist calibrated-vs-uncalibrated logloss improvement — closing both PARTIAL gaps from the Phase 2 verification report.

**Verified:** 2026-04-30
**Status:** human_needed
**Re-verification:** No — initial verification of Phase 02.1 (which itself is the re-verification of Phase 2's two PARTIAL gaps)
**Test run:** `uv run pytest tests/ -q` → **126 passed** in 73.61s, 0 failed (55 sklearn LGBMClassifier feature-name warnings; pre-existing noise on synthetic data)
**Git HEAD:** 78d0fdc

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1   | TrainingPipeline.run() invokes `apply_slippage` + `compute_clv` per test row, with `simulate_pick` selecting picks via the shared 5% edge filter (D-09, D-10) | VERIFIED | `pipeline.py:20-26` imports `EDGE_THRESHOLD_PCT, SLIPPAGE_PCT, apply_slippage, compute_clv, simulate_pick`; `pipeline.py:204-213` calls all three per test row inside the fold loop; replaces the Phase 2 placeholder at line 99. |
| 2   | Per-fold CLV is recorded in `walk_forward_fold_details` with the D-11 ≥20-picks gate; sub-threshold folds emit `clv_pct=None` | VERIFIED | `pipeline.py:215-217`: `fold_clv_pct = float(np.mean(fold_clvs)) if n_picks >= 20 else None`. Fold dict at `pipeline.py:225-233` includes `n_picks`, `clv_pct`, `logloss_uncalibrated`, `logloss_calibrated`. |
| 3   | `walk_forward_mean_clv_pct` is computed across qualifying folds and persisted to ModelMetadata (nullable when no fold reaches threshold) | VERIFIED | `pipeline.py:268-274` aggregates non-None CLVs; `metadata.py:37` types the field `float \| None`; written at `pipeline.py:295`. |
| 4   | `seed_historical.py` writes match results (home_goals/away_goals/status) to `ParquetStore.write_results()` from the `/fixtures` payload — zero extra API calls (D-06) | VERIFIED | `scripts/seed_historical.py:393-417` extracts `goals.home/away` + `status.short` from existing `/fixtures` response and writes via `store.write_results(result_row, overwrite=False)`. |
| 5   | `seed_historical.py` issues one bulk `/odds?league&season` call per (league, season), parses Betano 1X2 entries, writes to `ParquetStore.write_odds()` | VERIFIED | `seed_historical.py:336-360` calls `client.get_odds(league_id=..., season=season_year, bookmaker="Betano")`; `_parse_odds_entry()` at lines 231-288 extracts Betano Match Winner odds with closing-as-opening proxy (D-02) and null Pinnacle columns (D-04). |
| 6   | `ApiFootballClient.get_odds()` exists with dual-mode dispatch (per-fixture vs bulk-per-season) and tenacity retry stack | VERIFIED | `src/bip/sports/football/client.py:181-223`: `get_odds(fixture_id=None, league_id=None, season=None, bookmaker="Betano")` dispatches by xor; raises `ValueError` on neither; `@retry` decorator matches the other 5 endpoints. |
| 7   | `ModelMetadata` carries three nullable logloss fields and `walk_forward_mean_clv_pct` is `float \| None` | VERIFIED | `metadata.py:37` (`walk_forward_mean_clv_pct: float \| None`); `metadata.py:51-53` (`logloss_uncalibrated: float \| None = None`, `logloss_calibrated: float \| None = None`, `logloss_improvement_pct: float \| None = None`). |
| 8   | TrainingPipeline computes `log_loss(labels=[0,1,2])` BEFORE and AFTER calibration on the last fold; persists both + delta to ModelMetadata (D-13) | VERIFIED | `pipeline.py:252-264`: `raw_probs_last`, `cal_probs_last`, `logloss_uncal = log_loss(...labels=[0,1,2])`, `logloss_cal = log_loss(...labels=[0,1,2])`, `logloss_improvement_pct = (uncal-cal)/uncal*100`. Written at `pipeline.py:307-309`. |
| 9   | `EDGE_THRESHOLD_PCT = 0.05` constant + `simulate_pick(probs, opening_odds)` pure function exists in `bip.train.backtest` (D-09, D-10) | VERIFIED | `backtest.py:12` (`EDGE_THRESHOLD_PCT: float = 0.05`); `backtest.py:29-58` (`simulate_pick` returns `int \| None` with NaN-guard for D-03 Betano coverage gaps). |
| 10  | Seed checkpoint tracks three independent fixture-id sets (features / results / odds) with backward compat for Phase 2 single-key files | VERIFIED | `seed_historical.py:72-76` (`CHECKPOINT_KEYS`); `seed_historical.py:91-123` (`load_checkpoint()` with WR-03 schema_version reset); `tests/test_seed_checkpoint.py` carries 6 tests including `test_checkpoint_backward_compat_with_old_shape` and `test_pre_cr01_checkpoint_resets_results_and_odds`. |
| 11  | Migration 003 (is_shadow column + idx_predictions_is_shadow) is applied to live Supabase project hpcbctvcmcijwjvzecvx (D-15) | VERIFIED | Per `02.1-01-SUMMARY.md`: applied via Supabase MCP `apply_migration`; SQL probes confirmed `predictions.is_shadow` (data_type=boolean, is_nullable=NO, column_default=false) and `idx_predictions_is_shadow` exist in live DB. `supabase_migrations.schema_migrations` records all three migrations (20260502040134/40150/40156). Bonus: `supabase/migrations/20260101000000_base_schema.sql` was reconstructed from Pydantic models because the live project was empty (deviation 1 in 02.1-01-SUMMARY). |

**Score:** 11/11 truths VERIFIED at the code/structural level.

### Deferred Items (from Step 9b — addressed in later phases)

| #   | Item | Addressed In | Evidence |
| --- | ---- | ------------ | -------- |
| 1   | Empirical D-16 PL smoke train: `walk_forward_mean_clv_pct` is non-zero float, ≥3/5 folds have non-None `clv_pct`, `logloss_improvement_pct > 0` | Phase 3 prep | Plan 02.1-12 `status: deferred-verification` (commit 43f7544). User explicitly accepted risk per the plan's frontmatter to avoid spending ~1,520 API-Football Pro credits mid-phase. Code path proven on synthetic data via Plan 02.1-11 commit 012fb31. |
| 2   | Empirical confirmation of bulk /odds?league&season research assumptions (A1: returns historical data; A2: pagination shape; D-02: Betano present) | Phase 3 prep | Plan 02.1-02 `status: completed-with-deferred-verification` (commit 11806af). Script committed at `scripts/probe_odds_bulk.py` but not exercised. User accepted risk per the plan's `status: completed-with-deferred-verification` posture. |

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/bip/train/pipeline.py` | TrainingPipeline.run() invokes simulate_pick + apply_slippage + compute_clv per fold; computes logloss before/after calibration; writes all fields to ModelMetadata | VERIFIED | 324 lines. Full CLV+logloss wiring at lines 197-274. WR-01 empty-store guards at lines 107-121 (post-fix commit 9981044). |
| `src/bip/train/backtest.py` | EDGE_THRESHOLD_PCT constant + simulate_pick() function alongside Phase 2 SLIPPAGE_PCT/apply_slippage/compute_clv | VERIFIED | 59 lines. Constants at lines 11-12; simulate_pick at lines 29-58 with NaN-guard. |
| `src/bip/train/metadata.py` | ModelMetadata extended with logloss_uncalibrated, logloss_calibrated, logloss_improvement_pct (nullable); walk_forward_mean_clv_pct now nullable | VERIFIED | 59 lines. Three new fields at 51-53; walk_forward_mean_clv_pct at line 37 typed `float \| None`. |
| `src/bip/core/storage/parquet_store.py` | write_results / read_results / write_odds / read_odds with 3-level Hive partitioning (sport/league/season); _write accepts overwrite kwarg (CR-01 fix) | VERIFIED | 348 lines. Constants at 51-53; public APIs at 115-188; CR-01 fix in `_write` lines 246-288 (append-with-merge for overwrite=False). |
| `src/bip/sports/football/client.py` | ApiFootballClient.get_odds() with fixture / bulk dual-mode dispatch + ValueError | VERIFIED | 224 lines. get_odds at 181-223 with explicit retry decorator and security comment "log params dict only … api_key lives in x-apisports-key header set during __init__, never in logs". |
| `src/bip/sports/football/features.py` | feature_schema_version=2 stamped on every to_parquet_row output | VERIFIED | Line 224: `"feature_schema_version": [2]`. ParquetStore.read_features warns when column absent (lines 82-88 of parquet_store.py). |
| `src/bip/clv/client.py` | OddsApiClient.fetch_historical_closing() stub returns None + structlog warning (D-01 deferred) | VERIFIED | Lines 115-135. No @retry decorator on stub (per plan decision). Logs `pinnacle_historical_not_implemented`. |
| `scripts/seed_historical.py` | seed_one_league() writes features + results + odds; 3-key checkpoint with backward compat; ALLOWED_LEAGUE_SLUGS allowlist (T-02.1-04) | VERIFIED | 576 lines. Allowlist at 201-209; bulk odds fetch at 336-360; results extraction at 393-423; odds extraction at 427-445; per-fixture writes use `overwrite=False` to satisfy CR-01 fix. |
| `scripts/verify_migration_003.py` | psycopg-direct verification of is_shadow column + idx_predictions_is_shadow index; URL-encoded password (WR-02 fix) | VERIFIED | 108 lines. `derive_db_url` uses `quote(db_password, safe='')`; sanitized_host uses `rsplit("@", 1)[-1]`; parameterized SQL queries. |
| `scripts/probe_odds_bulk.py` | One-shot reconnaissance script for /odds?league&season (T-02.1-01 + T-02.1-05 mitigated) | VERIFIED (committed) / DEFERRED (not exercised) | 134 lines. Script structurally complete; never run against live API per Plan 02.1-02 `status: completed-with-deferred-verification`. |
| `supabase/migrations/20260101000000_base_schema.sql` | Reconstructed base schema for fresh-project bootstrap (6 tables + indexes + FKs) | VERIFIED | 155 lines. Reconstructed from Pydantic models per `02.1-01-SUMMARY.md` deviation 1; applied via MCP. |
| `supabase/migrations/20260423000000_add_is_shadow.sql` | is_shadow column + idx_predictions_is_shadow index | VERIFIED (applied to live DB) | 9 lines, idempotent. Applied via Supabase MCP per Plan 02.1-01. Live DB confirmation via SQL probes (column boolean NOT NULL DEFAULT false; index exists). |
| `tests/test_training_pipeline_e2e.py` | Synthetic E2E gate for TrainingPipeline.run() asserting CLV+logloss WIRING (not values); 200-row dataset; ~10% NaN injection on opening_home | VERIFIED | 202 lines, 1 test class with 1 test passing. Asserts walk_forward_mean_clv_pct float-or-None, all logloss fields are float, null_clv_rows > 0, fold structure matches WalkForwardSplitter(n_splits=5). |
| `tests/test_backtest_simulate_pick.py` | ≥4 tests for simulate_pick + EDGE_THRESHOLD_PCT | VERIFIED | 6 tests passing: argmax, threshold, NaN guard, custom threshold, constant value, smoke import. |
| `tests/test_metadata_logloss.py` | ≥3 tests for ModelMetadata logloss fields + JSON round-trip | VERIFIED | 6 tests passing: defaults_to_none, accepts_float_values, to_dict_roundtrip_populated/none, phase_2_construction, negative_improvement. |
| `tests/test_parquet_store_results_odds.py` | ≥5 tests for write/read results+odds round-trip + CR-01 regression guards | VERIFIED | 11 tests passing: round-trip results/odds, 3-level guards, fixture_id Int64 dtype, features-still-4-level, **+4 CR-01 regression tests** (per_fixture_results_appends, per_fixture_odds_appends, default_overwrite_still_replaces, per_fixture_results_dedups). |
| `tests/test_apifootball_get_odds.py` | 3 tests for get_odds dual-mode + arg-validation rejection | VERIFIED | 3 tests passing: fixture_mode, bulk_mode, rejects_neither. |
| `tests/test_seed_checkpoint.py` | 3-key checkpoint + WR-03 schema_version regression tests | VERIFIED | 6 tests passing: resume, atomic_write, empty_when_missing, backward_compat_with_old_shape, **+2 WR-03 regression tests** (pre_cr01_resets_results_and_odds, post_cr01_round_trips_with_version). |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| TrainingPipeline.run() | bip.train.backtest (simulate_pick + apply_slippage + compute_clv + EDGE_THRESHOLD_PCT + SLIPPAGE_PCT) | `from bip.train.backtest import (EDGE_THRESHOLD_PCT, SLIPPAGE_PCT, apply_slippage, compute_clv, simulate_pick)` | WIRED | pipeline.py:20-26 |
| TrainingPipeline.run() | ParquetStore.read_results / read_odds | `store.read_results(...)`, `store.read_odds(...)` | WIRED | pipeline.py:105-106 |
| TrainingPipeline.run() | features ⋈ results ⋈ odds inner+left join on fixture_id | `df.join(df_results.filter(... == "finished").select(...), on="fixture_id", how="inner")` + odds left join | WIRED | pipeline.py:122-135 |
| TrainingPipeline.run() | ModelMetadata logloss + CLV fields | `ModelMetadata(walk_forward_mean_clv_pct=..., logloss_uncalibrated=..., logloss_calibrated=..., logloss_improvement_pct=...)` | WIRED | pipeline.py:295, 307-309 |
| TrainingPipeline.run() | sklearn.metrics.log_loss with explicit labels=[0,1,2] | `log_loss(y_test, probs, labels=[0, 1, 2])` | WIRED | pipeline.py:222, 255, 258 (research finding 2 honored) |
| seed_one_league() | ApiFootballClient.get_odds bulk mode | `await client.get_odds(league_id=..., season=season_year, bookmaker="Betano")` | WIRED | seed_historical.py:336-340 |
| seed_one_league() | ParquetStore.write_results (overwrite=False) | `store.write_results(result_row, overwrite=False)` | WIRED | seed_historical.py:417 |
| seed_one_league() | ParquetStore.write_odds (overwrite=False) | `store.write_odds(odds_row, overwrite=False)` | WIRED | seed_historical.py:439 |
| seed_one_league() | ALLOWED_LEAGUE_SLUGS frozenset (T-02.1-04 path-traversal guard) | `if league_cfg.slug not in ALLOWED_LEAGUE_SLUGS: raise ValueError(...)` | WIRED | seed_historical.py:323-327 |
| ParquetStore._write | append-with-merge path for overwrite=False (CR-01 fix) | reads existing partition → concats → dedups on fixture_id (or fixture_id+bookmaker for odds) → rewrites | WIRED | parquet_store.py:246-288 |
| FeatureEngineer.to_parquet_row | feature_schema_version=2 column | hardcoded literal `"feature_schema_version": [2]` | WIRED | features.py:224 |
| ParquetStore.read_features | feature_schema_version_missing structlog warning | conditional `logger.warning` when column absent on non-empty df | WIRED | parquet_store.py:82-88 |
| OddsApiClient.fetch_historical_closing | structlog warning + None return (D-01 stub) | `logger.warning("pinnacle_historical_not_implemented", ...); return None` | WIRED (stub) | clv/client.py:129-135 |
| verify_migration_003.py | psycopg connection to live Supabase via SUPABASE_URL + SUPABASE_DB_PASSWORD | `psycopg.connect(db_url)` after `derive_db_url(quote(db_password, safe=''))` | WIRED | verify_migration_003.py:32-67 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| TrainingPipeline.run → metadata.walk_forward_mean_clv_pct | `walk_forward_mean_clv_pct` | mean over fold_details[*].clv_pct (where each clv_pct = mean of compute_clv(apply_slippage(opening), closing) over picks where simulate_pick returns non-None) | YES on synthetic (≥20 picks per fold producible); empirically pending on real PL data | FLOWING (synthetic, Plan 02.1-11) / PENDING (real, Plan 02.1-12 deferred) |
| TrainingPipeline.run → metadata.logloss_improvement_pct | `logloss_improvement_pct = (uncal - cal) / uncal * 100` | sklearn.metrics.log_loss on raw vs calibrated probs over last fold's test slice | YES — verified by synthetic E2E test (assert isinstance(meta.logloss_improvement_pct, float)) | FLOWING |
| TrainingPipeline.run → metadata.null_clv_rows | `null_clv_rows_total` | accumulator incremented when simulate_pick returns None (NaN coverage or edge<threshold) or when closing[o_sel] is NaN | YES — synthetic test injects ~10% None and asserts null_clv_rows > 0 | FLOWING |
| seed_one_league → ParquetStore.write_results | result_row | `item["goals"]["home"]/["away"]` and `item["fixture"]["status"]["short"]` from API-Football /fixtures payload | UNTESTED (live API) — code path correct; await Plan 02.1-12 | DISCONNECTED (real API not exercised; deferred) |
| seed_one_league → ParquetStore.write_odds | odds_row | `_parse_odds_entry(odds_entry, ...)` extracts Betano Match Winner odds from bulk /odds response | UNTESTED (live API) — bulk endpoint research assumptions A1+A2 deferred (Plan 02.1-02) | DISCONNECTED (real API not exercised; deferred) |
| ParquetStore._write (overwrite=False) | merged_frames | reads existing partition Parquet files, concatenates with incoming df, dedups on fixture_id (or fixture_id+bookmaker) | YES — verified by 4 CR-01 regression tests in test_parquet_store_results_odds.py | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Full test suite | `uv run pytest tests/ -q` | 126 passed in 73.61s, 0 failed | PASS |
| Phase 02.1 dedicated tests | `uv run pytest tests/test_training_pipeline_e2e.py tests/test_backtest_simulate_pick.py tests/test_metadata_logloss.py tests/test_parquet_store_results_odds.py tests/test_apifootball_get_odds.py tests/test_seed_checkpoint.py -v` | 33 passed in 55.80s | PASS |
| Synthetic E2E exercises full TrainingPipeline.run() against tmp_path Parquet stores | (subset of above) `tests/test_training_pipeline_e2e.py::test_pipeline_produces_metadata_with_all_fields_populated` | PASSED — asserts CLV float-or-None, all 3 logloss fields are float, null_clv_rows > 0, 5 folds with proper structure | PASS |
| backtest.simulate_pick imports cleanly with the right shape | (covered by tests/test_backtest_simulate_pick.py::test_simulate_pick_is_importable_from_bip_train_backtest) | PASS | PASS |
| Phase 02.1 module imports (no circulars) | Test suite imports every modified module at collection time | All 126 tests collected, no import errors | PASS |
| Real D-16 smoke train | `uv run python scripts/seed_historical.py --seasons 2024-2025 --leagues premier_league && uv run python -m bip.train fit --league premier_league --version v1` | NOT RUN — Plan 02.1-12 deferred | SKIP → human-verify (item 3) |
| Live `verify_migration_003.py` execution | `SUPABASE_DB_PASSWORD=... uv run python scripts/verify_migration_003.py` | NOT RUN — session never had DB password populated; equivalent SQL queries run via MCP | SKIP → human-verify (item 1) |
| Real bulk /odds probe | `uv run python scripts/probe_odds_bulk.py --league-id 39 --season 2024` | NOT RUN — Plan 02.1-02 deferred | SKIP → human-verify (item 2) |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
| ----------- | -------------- | ----------- | ------ | -------- |
| ML-02 | 02.1-00, 02.1-02, 02.1-03, 02.1-05, 02.1-06, 02.1-07, 02.1-08, 02.1-09, 02.1-10, 02.1-11, 02.1-12 | Walk-forward backtesting uses opening odds (not closing) + 1-2% slippage assumption from day 1 | SATISFIED (structural) / PENDING-EMPIRICAL (real numbers) | Code path verified end-to-end on synthetic data (Truths 1-9). `apply_slippage(opening) → compute_clv(staked, closing_proxy)` invoked per pick per fold; SLIPPAGE_PCT=0.015, EDGE_THRESHOLD_PCT=0.05 shared with Phase 3. Empirical confirmation deferred to Plan 02.1-12 (Phase 3 prep). |
| ML-03 | 02.1-00, 02.1-04, 02.1-10, 02.1-11, 02.1-12 | Calibration strategy: Platt <300 / Isotonic >500; `calibrate_by_league()`; documented logloss improvement | SATISFIED (structural) / PENDING-EMPIRICAL (real numbers) | `select_calibrator` rule already verified in Phase 2; Phase 02.1 adds the missing "documented logloss improvement" piece via `ModelMetadata.logloss_uncalibrated/logloss_calibrated/logloss_improvement_pct` populated by `pipeline.py:307-309`. Synthetic E2E test asserts all three are floats. Empirical per-league numbers deferred to Plan 02.1-12. |

REQUIREMENTS.md was updated to mark **ML-02** and **ML-03** as `[x]` (commit visible in diff). All requirement IDs declared in plan frontmatter are accounted for. No orphaned requirement IDs found.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | — | — | All 7 in-scope code-review findings (CR-01 + WR-01..06) fixed before verification. See `02.1-REVIEW-FIX.md` (commits c7bf71e, 9981044, 5ea19d2, 0101901, bd469d2, 7559f35, c3a3ece). |
| `src/bip/train/pipeline.py` | 64 | `_EnsembleProbaWrapper` re-fits the last fold's base models on a tiny slice (`X_fit[:1]`) to satisfy FrozenEstimator's API | INFO (deferred per Phase 02.1 specifics §1) | Documented as a known ergonomic wart in 02.1-CONTEXT.md `<specifics>`. Calibration math is correct; the wrapper ends up wrapping a correctly-fit ensemble. Refactor deferred to a separate task. |
| `src/bip/clv/client.py` | 115-135 | `fetch_historical_closing` returns None instead of raising NotImplementedError | INFO (intentional plumbing stub per D-01) | Deliberate — D-01 plumbing stub. Logs `pinnacle_historical_not_implemented` for observability; callers handle the missing-Pinnacle path without try/except boilerplate. |

No blockers. The two `INFO` items are both intentional deferrals documented in CONTEXT.md.

### Phase 02.1 Code Review Trail

| Finding | Severity | Description | Fix Commit |
| ------- | -------- | ----------- | ---------- |
| CR-01 | Critical | ParquetStore._write deletes entire partition on every call → per-fixture writes wipe prior data | c7bf71e (overwrite=False kwarg + append-with-merge) |
| WR-01 | Warning | pipeline.run() crashes with cryptic Polars error on fresh installs (empty stores) | 9981044 (empty-store guards before joins) |
| WR-02 | Warning | derive_db_url() does not URL-encode password — special chars break sanitization | 5ea19d2 (urllib.parse.quote + rsplit) |
| WR-03 | Warning | Seed checkpoint accounting drifts from partition store after CR-01 reseed | 0101901 (CHECKPOINT_SCHEMA_VERSION marker) |
| WR-04 | Warning | _parse_matchday returns 0 for non-numeric rounds, collapsing cup matches | bd469d2 (returns None; caller skips) |
| WR-05 | Warning | _pressing_tactical substring matchers race on multiple matching API stat names | 7559f35 (exact whitelist match) |
| WR-06 | Warning | SQL DEFAULT now() vs Pydantic nullability mismatch | c3a3ece (always emit captured_at + extra="ignore") |

**7 of 7 in-scope findings fixed.** 6 Info findings deferred per `fix_scope=critical_warning`. **+6 regression tests** added (4 CR-01 in test_parquet_store_results_odds.py, 2 WR-03 in test_seed_checkpoint.py). Test count moved from 120 (pre-fix baseline) to **126 passing**.

### Human Verification Required

Three items, all documented as deferred user-accepted risk in plan summaries:

1. **Run `scripts/verify_migration_003.py` against live Supabase** with `SUPABASE_DB_PASSWORD` set — script-level evidence checkbox. The migration IS applied (verified via Supabase MCP equivalent SQL during Plan 02.1-01); only the script-execution log line is missing.

2. **Run `scripts/probe_odds_bulk.py --league-id 39 --season 2024`** against live API-Football — confirms research assumptions A1 (bulk returns historical) + A2 (pagination) + Betano presence. Plan 02.1-02 risk-accepted by user.

3. **Run real Premier League 2024-2025 D-16 smoke train** end-to-end — produces empirical `metadata.json` with non-zero `walk_forward_mean_clv_pct`, ≥3/5 folds with non-None clv_pct, and `logloss_improvement_pct > 0`. Plan 02.1-12 risk-accepted by user (deferred to Phase 3 prep to avoid spending ~1,520 API-Football Pro credits mid-phase).

### Gaps Summary (Narrative)

**Phase 02.1 closes both PARTIAL gaps from Phase 2 verification at the structural level.** Every Phase 2 missing item is now present in code:

- The CLV math primitives (`apply_slippage`, `compute_clv`) are now invoked from `TrainingPipeline.run()` per test row per fold, gated by the 5% edge filter via the new `simulate_pick(probs, opening_odds)` shared with Phase 3's pick engine. The Phase 2 "placeholder" comment at pipeline.py:99 is replaced by the actual computation. `walk_forward_mean_clv_pct` is now computed from real fold-level CLVs (or `None` if no fold qualifies under the D-11 ≥20-picks gate).
- The `seed_historical.py` script now writes `results/` (home_goals/away_goals/status from existing /fixtures payload — zero extra API calls per D-06) and `odds/` (Betano 1X2 from one bulk /odds call per league-season) Parquet stores via the new 3-level Hive-partitioned `ParquetStore.write_results()` and `write_odds()` methods. The 3-key checkpoint allows independent resume of features / results / odds writes.
- `ModelMetadata` carries three new nullable logloss fields (`logloss_uncalibrated`, `logloss_calibrated`, `logloss_improvement_pct`) populated by a new `log_loss(labels=[0,1,2])` call on the last fold's test slice both before and after calibration. Per-fold uncalibrated logloss is recorded in `walk_forward_fold_details`; the last fold's calibrated logloss is back-filled.
- Migration 003 (`is_shadow` column + `idx_predictions_is_shadow` index) is applied to live Supabase project hpcbctvcmcijwjvzecvx via Supabase MCP; the equivalent `information_schema.columns` and `pg_indexes` SQL queries the verifier script runs were executed and returned the expected rows. Bonus: a base-schema migration (`20260101000000_base_schema.sql`) was reconstructed from Pydantic models because the live project was empty — Phase 2's ML-05 shadow-write path would have failed silently otherwise.
- All 7 code-review findings (1 Critical CR-01 + 6 Warnings) were fixed before verification and the test suite grew from 120 to **126 passing** with 6 regression tests guarding the fixes.

**The two outstanding deferrals are empirical, not structural:** Plan 02.1-12 (real PL smoke train producing the actual non-zero CLV in metadata.json) and Plan 02.1-02 (one-shot bulk /odds probe against the live API-Football endpoint). Both are explicitly documented in their plan summaries as `status: deferred-verification` / `completed-with-deferred-verification` with user risk-acceptance to avoid spending Pro-tier API-Football credits during Phase 02.1 execution. Both are explicitly addressed in Phase 3 prep, which cannot proceed without them. The `human_needed` status reflects these deferrals, not unresolved Phase 02.1 work.

**Recommendation:** Phase 02.1 is structurally complete and ready for closure. Before Phase 3 implementation begins, run the three human-verification items above as Phase 3 prep — at that point ML-02 and ML-03 transition from "structurally PASS" to "empirically PASS" and `02-VERIFICATION.md` `status: resolved_pending_empirical` becomes `status: resolved`.

---

_Verified: 2026-04-30_
_Verifier: Claude (gsd-verifier)_
_Test run: `uv run pytest tests/ -q` → 126 passed, 0 failed, 73.61s_
_Git HEAD: 78d0fdc_
