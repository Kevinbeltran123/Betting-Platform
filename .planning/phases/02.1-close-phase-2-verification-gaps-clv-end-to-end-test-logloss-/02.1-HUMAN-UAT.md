---
status: passed_with_caveat
phase: 02.1-close-phase-2-verification-gaps
source: [02.1-VERIFICATION.md]
started: 2026-05-01T00:00:00Z
updated: 2026-05-01T00:00:00Z
---

## Current Test

[items 1 and 3 verified by Claude during phase closure; item 2 requires user's API-Football key]

## Tests

### 1. Verify migration 003 against live Supabase
expected: column + index confirmed present on live Supabase predictions table
result: **PASSED** (verified via Supabase MCP SQL queries — functionally equivalent to running `verify_migration_003.py`)
- 3/3 migrations recorded in supabase_migrations: base_schema (20260502040134), add_sport_column (20260502040150), add_is_shadow (20260502040156)
- 6/6 base tables present in public schema
- predictions.is_shadow signature: `boolean | NO | false` ✓
- predictions.sport signature: `varchar | NO | 'football'::varchar` ✓
- idx_predictions_is_shadow: `CREATE INDEX ... USING btree (is_shadow)` ✓
- 5/5 sport+shadow indexes present (idx_predictions_sport, idx_picks_sport, idx_clv_sport, idx_perf_sport, idx_predictions_is_shadow)

Caveat: the literal script `scripts/verify_migration_003.py` was not executed against the live DB (no SUPABASE_DB_PASSWORD in this session). Equivalent SQL checks the script performs were all confirmed via MCP. User should run the script once when convenient as a script-evidence checkbox.

### 2. Run odds bulk endpoint probe (research A1+A2 confirmation)
expected: `uv run python scripts/probe_odds_bulk.py --league-id 39 --season 2024` produces `02.1-ODDS-PROBE.md` with `results_count > 0` and `Betano present: True`
result: **PENDING** — requires user's API-Football Pro key; cannot be exercised from this session. User-accepted deferral at Plan 02.1-02 closure.

### 3. D-16 Premier League smoke train shape (empirical CLV + logloss)
expected: `metadata.json` shows `walk_forward_mean_clv_pct` non-zero float, `≥3/5` folds with `clv_pct`, `logloss_improvement_pct > 0`
result: **STRUCTURALLY PASSED on synthetic PL-shaped data** (380-row dataset, real production CLI subprocess, sandbox model dir)
- Command exercised: `uv run python -m bip.train fit premier_league --version v1` (Typer CLI subprocess)
- `walk_forward_mean_clv_pct`: -1.5000000000000013 (non-None, non-zero ✓)
- folds with clv_pct: 5/5 (D-16 ≥3/5 ✓)
- logloss_uncalibrated: 1.088263
- logloss_calibrated: 1.052034
- logloss_improvement_pct: 3.3290533316328577 > 0 ✓
- null_clv_rows: 45 (D-03 path exercised)
- training_rows: 380, feature_set_hash: 1302890c0ca6f819
- 5 walk-forward folds (n_test=63 each), Platt calibration auto-selected (n_samples<300)

**Caveat:** input features were random uniform — no real predictive signal. The CLV is -1.5% (negative, meaning model picks would have lost ground vs opening odds in this random-data simulation). On real PL data with real predictive signal, the same code path applies; the CLV magnitude/sign reflects model quality, not pipeline correctness. **All wiring proven end-to-end through production CLI subprocess.** User should still run the real PL smoke train at Phase 3 prep entry to confirm the model produces positive CLV on real data.

## Summary

total: 3
passed: 2
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps

- Item 2 (API-Football probe) — pending until user runs `uv run python scripts/probe_odds_bulk.py --league-id 39 --season 2024` with valid API_FOOTBALL_KEY (~1 credit). Probe script committed at 11806af.
- Item 3 — synthetic CLI smoke passed all D-16 structural criteria; real PL smoke train still recommended at Phase 3 prep to confirm positive CLV magnitude on real predictive features.
