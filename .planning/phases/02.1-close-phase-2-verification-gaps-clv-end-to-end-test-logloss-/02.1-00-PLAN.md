---
phase: 02.1
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_training_pipeline_e2e.py
  - tests/test_backtest_simulate_pick.py
  - tests/test_metadata_logloss.py
  - tests/test_parquet_store_results_odds.py
  - tests/test_apifootball_get_odds.py
  - scripts/verify_migration_003.py
autonomous: true
requirements: [ML-02, ML-03]
tags: [testing, wave-0, stubs]

must_haves:
  truths:
    - "All 6 Wave 0 test/script stubs exist at the paths listed in VALIDATION.md"
    - "Each pytest stub is collectable (pytest --collect-only succeeds) and currently skips/xfails"
    - "scripts/verify_migration_003.py exists and exits with a non-zero status when SUPABASE_DB_PASSWORD is unset (stub behavior is acceptable)"
  artifacts:
    - path: "tests/test_training_pipeline_e2e.py"
      provides: "D-17 synthetic-gate test skeleton"
    - path: "tests/test_backtest_simulate_pick.py"
      provides: "D-09 / D-10 simulate_pick unit test skeleton"
    - path: "tests/test_metadata_logloss.py"
      provides: "D-13 ModelMetadata logloss-field round-trip skeleton"
    - path: "tests/test_parquet_store_results_odds.py"
      provides: "D-04 results/odds round-trip skeleton"
    - path: "tests/test_apifootball_get_odds.py"
      provides: "D-07 get_odds dispatch skeleton"
    - path: "scripts/verify_migration_003.py"
      provides: "D-15 migration verification entry point"
  key_links:
    - from: "downstream Wave 1+ plans"
      to: "Wave 0 stubs"
      via: "pytest file-path references in <automated> blocks"
      pattern: "tests/test_.*\\.py"
---

<objective>
Create empty test/script skeletons so every downstream task's `<automated>` command can resolve its target file. All stubs use `pytest.skip("Phase 02.1 Wave 0 stub")` (test files) or graceful env-var exit (script). Wave 1+ plans replace the skip markers with real assertions.

Purpose: satisfy the Nyquist sampling invariant — every task in Waves 1-4 must reference an existing test path or risk `pytest: file not found`. Also produces the `scripts/verify_migration_003.py` entry point so Plan 02.1-01 only needs to fill in the body.

Output: 6 new files (5 pytest + 1 python script), all collectable and runnable in `tests/` / `scripts/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-close-phase-2-verification-gaps-clv-end-to-end-test-logloss-/02.1-CONTEXT.md
@.planning/phases/02.1-close-phase-2-verification-gaps-clv-end-to-end-test-logloss-/02.1-RESEARCH.md
@.planning/phases/02.1-close-phase-2-verification-gaps-clv-end-to-end-test-logloss-/02.1-PATTERNS.md
@.planning/phases/02.1-close-phase-2-verification-gaps-clv-end-to-end-test-logloss-/02.1-VALIDATION.md

<interfaces>
<!-- Stub conventions already established in this repo -->

From tests/conftest.py — the `settings` fixture uses pydantic Settings:
```python
@pytest.fixture
def settings():
    from bip.core.settings import Settings
    return Settings(_env_file=None)
```

From tests/test_parquet_store.py — the tmp_path+ParquetStore fixture pattern:
```python
@pytest.fixture
def store(tmp_path):
    from bip.core.storage.parquet_store import ParquetStore
    return ParquetStore(base_path=tmp_path)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create all five pytest stubs + verify_migration_003.py skeleton</name>
  <files>
    tests/test_training_pipeline_e2e.py,
    tests/test_backtest_simulate_pick.py,
    tests/test_metadata_logloss.py,
    tests/test_parquet_store_results_odds.py,
    tests/test_apifootball_get_odds.py,
    scripts/verify_migration_003.py
  </files>
  <read_first>
    tests/test_parquet_store.py (for tmp_path fixture pattern, lines 1-30),
    tests/test_api_football_client.py (for httpx_mock fixture pattern, lines 1-20),
    tests/test_backtest_clv.py (for TestBacktestClv structural pattern, lines 1-35),
    tests/conftest.py (for settings fixture),
    scripts/seed_historical.py (for argparse + structlog + __main__ skeleton, lines 1-40),
    .planning/phases/02.1-close-phase-2-verification-gaps-clv-end-to-end-test-logloss-/02.1-PATTERNS.md (section "tests/test_training_pipeline_e2e.py (CREATE)" and section "scripts/verify_migration_003.py (CREATE)")
  </read_first>
  <action>
Create six files. Every pytest stub uses `pytest.skip("Phase 02.1 Wave 0 stub — filled in by plan 02.1-NN")` at module scope so `pytest --collect-only` succeeds but no test actually runs. The verify_migration_003.py stub exits 1 when SUPABASE_DB_PASSWORD is missing and prints a stub marker otherwise.

**File 1: `tests/test_training_pipeline_e2e.py`**
```python
"""Phase 02.1 Wave 0 stub — filled in by plan 02.1-11 (synthetic e2e).

D-17 synthetic integration test: pipeline.run() on tmp_path Parquet stores.
"""
from __future__ import annotations

import pytest

pytestmark = pytest.mark.skip(
    reason="Phase 02.1 Wave 0 stub — plan 02.1-11 implements synthetic e2e test"
)


def test_pipeline_produces_metadata_with_all_fields_populated():
    """Placeholder — plan 02.1-11 writes the real assertions."""
    pass
```

**File 2: `tests/test_backtest_simulate_pick.py`**
```python
"""Phase 02.1 Wave 0 stub — filled in by plan 02.1-03.

D-09, D-10: simulate_pick() + EDGE_THRESHOLD_PCT unit tests.
"""
from __future__ import annotations

import pytest

pytestmark = pytest.mark.skip(
    reason="Phase 02.1 Wave 0 stub — plan 02.1-03 implements simulate_pick tests"
)


def test_simulate_pick_returns_argmax_when_edge_clears():
    pass


def test_simulate_pick_returns_none_below_threshold():
    pass


def test_simulate_pick_returns_none_when_any_odds_nan():
    pass


def test_edge_threshold_default_0_05():
    pass
```

**File 3: `tests/test_metadata_logloss.py`**
```python
"""Phase 02.1 Wave 0 stub — filled in by plan 02.1-04.

D-13: ModelMetadata logloss fields serialize/deserialize round-trip.
"""
from __future__ import annotations

import pytest

pytestmark = pytest.mark.skip(
    reason="Phase 02.1 Wave 0 stub — plan 02.1-04 implements logloss field tests"
)


def test_model_metadata_logloss_fields_default_to_none():
    pass


def test_model_metadata_logloss_round_trip_json():
    pass
```

**File 4: `tests/test_parquet_store_results_odds.py`**
```python
"""Phase 02.1 Wave 0 stub — filled in by plan 02.1-05.

D-04: ParquetStore.write_results / read_results / write_odds / read_odds
with 3-level Hive partitioning (sport/league/season; NO matchday).
"""
from __future__ import annotations

import pytest

pytestmark = pytest.mark.skip(
    reason="Phase 02.1 Wave 0 stub — plan 02.1-05 implements results/odds round-trip tests"
)


def test_round_trip_results():
    pass


def test_round_trip_odds():
    pass


def test_results_is_3_level_not_4_level():
    pass


def test_odds_is_3_level_not_4_level():
    pass
```

**File 5: `tests/test_apifootball_get_odds.py`**
```python
"""Phase 02.1 Wave 0 stub — filled in by plan 02.1-06.

D-07 (adjusted per research finding 1): get_odds dispatch between
per-fixture mode and bulk (league+season) mode.
"""
from __future__ import annotations

import pytest

pytestmark = pytest.mark.skip(
    reason="Phase 02.1 Wave 0 stub — plan 02.1-06 implements get_odds dispatch tests"
)


async def test_get_odds_fixture_mode():
    pass


async def test_get_odds_bulk_mode():
    pass


async def test_get_odds_rejects_neither():
    pass
```

**File 6: `scripts/verify_migration_003.py`** — skeleton. Plan 02.1-01 fills in the psycopg query body.
```python
"""Verify migration 003 (is_shadow column + index) is applied to live Supabase.

Phase 02.1 Wave 0 stub — plan 02.1-01 fills in the psycopg verification body.

Exit criterion for Phase 02.1 (D-15). Returns exit code 0 on success, 1 on failure.
"""
from __future__ import annotations

import os
import sys


def main() -> int:
    """Stub — real implementation lands in plan 02.1-01."""
    if not os.environ.get("SUPABASE_DB_PASSWORD"):
        print("FAIL: SUPABASE_DB_PASSWORD not set in environment")
        return 1
    print(
        "STUB: Phase 02.1 Wave 0 — plan 02.1-01 will implement the real "
        "information_schema and pg_indexes queries here."
    )
    return 1  # Stub never claims success


if __name__ == "__main__":
    sys.exit(main())
```

Naming rationale: the CONTEXT and VALIDATION documents reference both short names (e.g. `test_backtest_clv.py`) and longer variants (`test_backtest_simulate_pick.py`). We create the new dedicated file `tests/test_backtest_simulate_pick.py` for simulate_pick tests (plan 02.1-03) while leaving the existing `tests/test_backtest_clv.py` untouched. Same for `tests/test_parquet_store_results_odds.py` vs the existing `tests/test_parquet_store.py`, and `tests/test_apifootball_get_odds.py` vs `tests/test_api_football_client.py`. This avoids accidental edits to Phase 2 tests and keeps each plan's diff surface minimal.

Use the Write tool for each file. Do NOT create `__init__.py` in `tests/` — it already exists.
  </action>
  <verify>
    <automated>uv run pytest tests/test_training_pipeline_e2e.py tests/test_backtest_simulate_pick.py tests/test_metadata_logloss.py tests/test_parquet_store_results_odds.py tests/test_apifootball_get_odds.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_training_pipeline_e2e.py` exits 0
    - `test -f tests/test_backtest_simulate_pick.py` exits 0
    - `test -f tests/test_metadata_logloss.py` exits 0
    - `test -f tests/test_parquet_store_results_odds.py` exits 0
    - `test -f tests/test_apifootball_get_odds.py` exits 0
    - `test -f scripts/verify_migration_003.py` exits 0
    - `grep -c "Phase 02.1 Wave 0 stub" tests/test_training_pipeline_e2e.py tests/test_backtest_simulate_pick.py tests/test_metadata_logloss.py tests/test_parquet_store_results_odds.py tests/test_apifootball_get_odds.py scripts/verify_migration_003.py | awk -F: '{s+=$2} END {print s}'` prints a number ≥ 6
    - `uv run pytest tests/test_training_pipeline_e2e.py tests/test_backtest_simulate_pick.py tests/test_metadata_logloss.py tests/test_parquet_store_results_odds.py tests/test_apifootball_get_odds.py --collect-only -q` exits 0
    - `SUPABASE_DB_PASSWORD='' uv run python scripts/verify_migration_003.py; test $? -eq 1` succeeds (stub must fail without the env var)
    - `uv run pytest tests/ -q` exits 0 (no regressions in existing Phase 2 tests)
  </acceptance_criteria>
  <done>
    All six files created. Collect-only passes. The full existing test suite still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None in this plan | Wave 0 creates test/script skeletons only; no untrusted input, no secrets, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-02 | Information Disclosure | scripts/verify_migration_003.py (future body) | mitigate | Stub body in this plan only reads `SUPABASE_DB_PASSWORD` via `os.environ.get` (never logs it) and does not print the full value; plan 02.1-01 carries the real mitigation forward. |
</threat_model>

<verification>
`uv run pytest tests/ -q` — full suite green (existing 95+ tests unchanged, new 5 stubs skip cleanly).
`ls -1 tests/test_training_pipeline_e2e.py tests/test_backtest_simulate_pick.py tests/test_metadata_logloss.py tests/test_parquet_store_results_odds.py tests/test_apifootball_get_odds.py scripts/verify_migration_003.py` — all 6 listed.
</verification>

<success_criteria>
- 5 new pytest stubs + 1 new script skeleton exist at the paths listed in `files_modified`
- `pytest --collect-only` succeeds for all 5 stubs
- No regressions: `uv run pytest tests/ -q` still green
- `scripts/verify_migration_003.py` is importable as a module and exits 1 without `SUPABASE_DB_PASSWORD`
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-close-phase-2-verification-gaps-clv-end-to-end-test-logloss-/02.1-00-SUMMARY.md`
</output>
