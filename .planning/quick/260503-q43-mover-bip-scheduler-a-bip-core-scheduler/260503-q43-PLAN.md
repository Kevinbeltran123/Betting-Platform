---
phase: 260503-q43
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bip/scheduler/__init__.py
  - src/bip/scheduler/orchestrator.py
  - src/bip/core/scheduler/__init__.py
  - src/bip/core/scheduler/orchestrator.py
  - tests/test_scheduler.py
  - tests/scheduler/test_orchestrator.py
  - tests/scheduler/test_reconcile.py
  - scripts/smoke_e2e_runbook.md
autonomous: true
requirements:
  - G-CODE-04
  - G-CODE-10
  - CORE-02
must_haves:
  truths:
    - "src/bip/scheduler/ no longer exists"
    - "src/bip/core/scheduler/ exists with __init__.py and orchestrator.py"
    - "git history for orchestrator.py is preserved via git mv (rename, not delete+create)"
    - "All imports of bip.scheduler.* are replaced with bip.core.scheduler.*"
    - "Full pytest suite (≥292 tests) passes with zero failures"
    - "No leftover references to the old bip.scheduler path anywhere in src/, tests/, or scripts/"
  artifacts:
    - path: "src/bip/core/scheduler/__init__.py"
      provides: "Package init for relocated scheduler"
    - path: "src/bip/core/scheduler/orchestrator.py"
      provides: "PipelineOrchestrator class (relocated, content unchanged)"
  key_links:
    - from: "tests/test_scheduler.py"
      to: "bip.core.scheduler.orchestrator"
      via: "import statement"
      pattern: "from bip\\.core\\.scheduler\\.orchestrator import PipelineOrchestrator"
    - from: "tests/scheduler/test_orchestrator.py"
      to: "bip.core.scheduler.orchestrator"
      via: "import statement"
      pattern: "bip\\.core\\.scheduler\\.orchestrator"
    - from: "tests/scheduler/test_reconcile.py"
      to: "bip.core.scheduler.orchestrator"
      via: "import statement"
      pattern: "from bip\\.core\\.scheduler\\.orchestrator import PipelineOrchestrator"
---

<objective>
Relocate the scheduler package from `src/bip/scheduler/` to `src/bip/core/scheduler/` to satisfy the contract prescribed in `.planning/research/ARCHITECTURE.md` (Component Boundaries section) and close audit gaps G-CODE-04 and G-CODE-10. After the move, CORE-02 ("core/ contains zero sport-specific code, all sport-agnostic glue") flips from Partial to Complete because the scheduler — which is sport-agnostic glue — now lives under core/. Phase 4 modules (`core/health.py`, `core/metrics.py`, `core/pipeline.py`) will land alongside it.

Purpose: Mechanical structural correction. No behavior change.
Output: Relocated package with preserved git history, updated imports in 3 test files + 1 doc, full test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/AUDIT-GAPS.md
@.planning/research/ARCHITECTURE.md
@src/bip/scheduler/__init__.py
@src/bip/scheduler/orchestrator.py
@tests/test_scheduler.py
@tests/scheduler/test_orchestrator.py
@tests/scheduler/test_reconcile.py
@scripts/smoke_e2e_runbook.md
@pyproject.toml

<interfaces>
<!-- Confirmed call site inventory (grep -rn "bip\.scheduler" src/ tests/ scripts/) -->
<!-- ZERO src/ imports of bip.scheduler — orchestrator is constructed externally. -->

Test files (3) — total ≈26 import sites:
- tests/test_scheduler.py: 4 hits, all `from bip.scheduler.orchestrator import PipelineOrchestrator`
- tests/scheduler/test_reconcile.py: 2 hits, all `from bip.scheduler.orchestrator import PipelineOrchestrator`
- tests/scheduler/test_orchestrator.py: 19 hits — mix of:
  - `from bip.scheduler.orchestrator import PipelineOrchestrator`
  - `import bip.scheduler.orchestrator as mod`

Doc (1):
- scripts/smoke_e2e_runbook.md:101 — `from bip.scheduler.orchestrator import PipelineOrchestrator`

Build config:
- pyproject.toml uses `packages = ["src/bip"]` (package discovery) — NO manifest changes required.

Current scheduler __init__.py contents:
```python
"""APScheduler 3.x pipeline orchestrator."""
```
(Docstring only — no re-exports. Move preserves contents byte-for-byte.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv scheduler under core/ + update all import sites</name>
  <files>
    src/bip/scheduler/ (moved to src/bip/core/scheduler/),
    tests/test_scheduler.py,
    tests/scheduler/test_orchestrator.py,
    tests/scheduler/test_reconcile.py,
    scripts/smoke_e2e_runbook.md
  </files>
  <action>
Execute the relocation in this exact order:

1. **Ensure parent directory exists**: `src/bip/core/` already exists (verified). No mkdir needed.

2. **Run `git mv` to preserve history** (single command, NOT Write+delete):
   ```
   git mv src/bip/scheduler src/bip/core/scheduler
   ```
   This renames the directory and stages the rename. Both `__init__.py` and `orchestrator.py` move together with their git history intact.

3. **Update test imports** — use `sed -i ''` (macOS BSD sed syntax) or the Edit tool to replace `bip.scheduler` with `bip.core.scheduler` in each file. Use word-boundary care: the only valid pattern is `bip.scheduler` (with literal dot). Do NOT touch any string that doesn't match this.

   Files to edit:
   - `tests/test_scheduler.py` — 4 occurrences of `from bip.scheduler.orchestrator import PipelineOrchestrator` → `from bip.core.scheduler.orchestrator import PipelineOrchestrator`
   - `tests/scheduler/test_reconcile.py` — 2 occurrences, same pattern
   - `tests/scheduler/test_orchestrator.py` — 19 occurrences, mix of:
     - `from bip.scheduler.orchestrator import PipelineOrchestrator` → `from bip.core.scheduler.orchestrator import PipelineOrchestrator`
     - `import bip.scheduler.orchestrator as mod` → `import bip.core.scheduler.orchestrator as mod`

   Suggested one-shot for the in-place replacement (BSD sed compatible):
   ```
   sed -i '' 's/bip\.scheduler/bip.core.scheduler/g' tests/test_scheduler.py tests/scheduler/test_orchestrator.py tests/scheduler/test_reconcile.py
   ```

4. **Update doc**: `scripts/smoke_e2e_runbook.md` line 101 — replace `from bip.scheduler.orchestrator import PipelineOrchestrator` with `from bip.core.scheduler.orchestrator import PipelineOrchestrator`. Use Edit tool for the doc (sed risks touching prose).

5. **Sanity-check pyproject.toml**: confirmed at plan time — uses `packages = ["src/bip"]` (discovery). No edit required. If a future change introduced an explicit list, this would also need updating, but it does not today.

6. **Do NOT modify** `src/bip/core/scheduler/__init__.py` contents — the docstring `"""APScheduler 3.x pipeline orchestrator."""` is preserved verbatim by `git mv`.

7. **Do NOT modify** `.planning/research/ARCHITECTURE.md` — it already prescribes this layout. The whole point of this work is to make the code match the doc.

8. **Single conventional commit**:
   ```
   refactor(core): move scheduler under core/ to match ARCHITECTURE.md (G-CODE-04, G-CODE-10)
   ```
   No Co-Authored-By trailer. No emojis. Stage only the renamed package + the 4 import-updated files.
  </action>
  <verify>
    <automated>
test -f src/bip/core/scheduler/__init__.py && \
test -f src/bip/core/scheduler/orchestrator.py && \
test ! -d src/bip/scheduler && \
[ "$(grep -rn 'bip\.scheduler' src/ tests/ scripts/ 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && \
[ "$(grep -rn 'bip\.core\.scheduler' src/ tests/ scripts/ 2>/dev/null | wc -l | tr -d ' ')" -ge "4" ] && \
git log --diff-filter=R --follow --oneline src/bip/core/scheduler/orchestrator.py | head -1 | grep -q . && \
uv run pytest -q 2>&1 | tail -3
    </automated>
  </verify>
  <done>
- `src/bip/scheduler/` directory removed.
- `src/bip/core/scheduler/__init__.py` and `src/bip/core/scheduler/orchestrator.py` exist with content unchanged from the original.
- `grep -rn "bip\.scheduler" src/ tests/ scripts/` returns ZERO hits.
- `grep -rn "bip\.core\.scheduler" src/ tests/ scripts/` returns ≥4 hits across the 3 test files + 1 doc.
- `git log --diff-filter=R --follow src/bip/core/scheduler/orchestrator.py` shows the rename commit (history preserved).
- `uv run pytest -q` reports ≥292 passed, 0 failed.
- One commit on the branch with message `refactor(core): move scheduler under core/ to match ARCHITECTURE.md (G-CODE-04, G-CODE-10)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure internal refactor — no new trust boundaries crossed. No user input, no network, no auth surface, no data flow change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q43-01 | Tampering | git history | mitigate | Use `git mv` (not Write+delete) so the rename is recorded as a rename, not a deletion + creation. Verified by `git log --diff-filter=R --follow`. |
| T-q43-02 | Denial of Service | test suite | mitigate | Run full `uv run pytest -q` after the move. Any leftover stale import will hard-fail at collection time, surfaced before commit. |
| T-q43-03 | Information Disclosure | n/a | accept | No secrets, no PII, no network surface touched. Pure module relocation. |
</threat_model>

<verification>
After Task 1 completes:

1. **Filesystem**:
   - `test ! -d src/bip/scheduler` — old directory gone
   - `test -d src/bip/core/scheduler` — new directory present
   - `test -f src/bip/core/scheduler/__init__.py` — init present
   - `test -f src/bip/core/scheduler/orchestrator.py` — orchestrator present

2. **Reference hygiene** (grep guards):
   - `grep -rn "bip\.scheduler" src/ tests/ scripts/` → 0 hits
   - `grep -rn "bip\.core\.scheduler" src/ tests/ scripts/` → ≥4 hits

3. **Git history preservation**:
   - `git log --diff-filter=R --follow --oneline src/bip/core/scheduler/orchestrator.py` shows at least one rename commit

4. **Test suite**:
   - `uv run pytest -q` → ≥292 passing, 0 failed

5. **No collateral damage**:
   - `git status` shows only the expected modifications: 2 renamed files + 4 modified files (3 tests + 1 doc)
   - No changes outside `src/bip/scheduler/`, `src/bip/core/scheduler/`, the 3 test files, and the runbook doc
</verification>

<success_criteria>
- Audit gaps G-CODE-04 and G-CODE-10 are mechanically resolved (scheduler is now under core/, matching ARCHITECTURE.md prescription).
- CORE-02 traceability flips from "Partial" to "Complete" (followup audit task, not in this plan).
- All 292 existing tests remain green.
- Git history for `orchestrator.py` is preserved (rename, not delete+create).
- Single atomic commit with conventional message format.
- Phase 4 can land `core/health.py`, `core/metrics.py`, `core/pipeline.py` as siblings of `core/scheduler/` without further restructuring.
</success_criteria>

<output>
After completion, create `.planning/quick/260503-q43-mover-bip-scheduler-a-bip-core-scheduler/260503-q43-SUMMARY.md` with:
- One-line outcome statement
- Files renamed (path-before → path-after)
- Files modified (with hit-count delta)
- Test result (passed/failed counts)
- Note: "CORE-02 traceability table can flip Partial → Complete in next audit pass."
- Commit SHA + message
</output>
