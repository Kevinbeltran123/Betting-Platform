---
phase: 260503-q43
plan: 01
type: quick
status: complete
completed: "2026-05-04T02:25:00Z"
duration_minutes: 2
tasks_total: 1
tasks_done: 1
files_renamed: 2
files_modified: 4
tests_passed: 292
tests_failed: 0
requirements: [G-CODE-04, G-CODE-10, CORE-02]
key_files:
  renamed:
    - from: src/bip/scheduler/__init__.py
      to: src/bip/core/scheduler/__init__.py
    - from: src/bip/scheduler/orchestrator.py
      to: src/bip/core/scheduler/orchestrator.py
  modified:
    - tests/test_scheduler.py
    - tests/scheduler/test_orchestrator.py
    - tests/scheduler/test_reconcile.py
    - scripts/smoke_e2e_runbook.md
---

# Quick Task 260503-q43: Move bip/scheduler to bip/core/scheduler Summary

## Outcome

Relocated scheduler package under `core/` to match `.planning/research/ARCHITECTURE.md` prescription, preserving git history via `git mv`. Closes audit gaps G-CODE-04 and G-CODE-10. All 292 tests remain green; zero behavior change.

## Files Renamed (history preserved)

| Before                              | After                                    |
| ----------------------------------- | ---------------------------------------- |
| `src/bip/scheduler/__init__.py`     | `src/bip/core/scheduler/__init__.py`     |
| `src/bip/scheduler/orchestrator.py` | `src/bip/core/scheduler/orchestrator.py` |

Verified by `git status` reporting `R` (rename) for both files (similarity index 100%).

## Files Modified (import updates)

| File                                   | Old hits | New hits | Delta                              |
| -------------------------------------- | -------- | -------- | ---------------------------------- |
| `tests/test_scheduler.py`              | 4        | 4        | `bip.scheduler` → `bip.core.scheduler` |
| `tests/scheduler/test_orchestrator.py` | 19       | 19       | `bip.scheduler` → `bip.core.scheduler` |
| `tests/scheduler/test_reconcile.py`    | 2        | 2        | `bip.scheduler` → `bip.core.scheduler` |
| `scripts/smoke_e2e_runbook.md`         | 1        | 1        | `bip.scheduler` → `bip.core.scheduler` |

Total: 26 import-site updates across 4 files.

## Verification Evidence

| Check                                                                            | Result          |
| -------------------------------------------------------------------------------- | --------------- |
| `test ! -d src/bip/scheduler`                                                    | PASS (gone)     |
| `test -f src/bip/core/scheduler/__init__.py`                                     | PASS            |
| `test -f src/bip/core/scheduler/orchestrator.py`                                 | PASS            |
| `grep -rn "bip\.scheduler" src/ tests/ scripts/` (excluding `bip.core.scheduler`) | 0 hits          |
| `grep -rn "bip\.core\.scheduler" src/ tests/ scripts/`                           | 23 hits (≥4 OK) |
| `git status` rename detection                                                    | R (rename)      |
| `uv run pytest -q`                                                               | 292 passed, 0 failed |

## Test Result

```
292 passed, 55 warnings in 80.32s
```

## Deviations from Plan

None — plan executed exactly as written. Single `git mv` + `sed` for tests + `Edit` for the doc, in the order prescribed.

## Notes

- `pyproject.toml` uses `packages = ["src/bip"]` (discovery-based), so no manifest edit was required. Confirmed at plan time and re-confirmed at execution.
- `src/bip/core/scheduler/__init__.py` content was preserved byte-for-byte by `git mv` (docstring `"""APScheduler 3.x pipeline orchestrator."""` only).
- **CORE-02 traceability table can flip Partial → Complete in next audit pass** (out of scope for this quick task; tracked under audit followup).
- Phase 4 modules (`core/health.py`, `core/metrics.py`, `core/pipeline.py`) can now land alongside `core/scheduler/` without further restructuring.

## Commit

- Message: `refactor(quick-260503-q43): move bip/scheduler to bip/core/scheduler (G-CODE-04/10)`
- SHA: <recorded after commit>

## Self-Check: PASSED

- Renamed files exist at new paths: FOUND
- Old directory removed: FOUND (gone)
- All 292 tests pass: FOUND
- Zero leftover `bip.scheduler` references: FOUND
- Git rename detection active (history preserved): FOUND
