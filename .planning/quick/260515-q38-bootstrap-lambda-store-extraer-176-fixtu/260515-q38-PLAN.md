---
phase: 260515-q38
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/spike/v3/build_history_from_snapshots.py
  - tests/evaluation/live/engine_v3/test_build_history_from_snapshots.py
autonomous: true
requirements:
  - V3-LAMBDA-BOOTSTRAP
must_haves:
  truths:
    - "A new script extracts FINISHED fixtures from data/cache/sportmonks/snapshots/<fid>/*.json using the EXISTING fixed Sportmonks parser (Fixture.model_validate -> LiveMatchState.from_fixture), NOT hand-rolled JSON parsing (avoids re-introducing the home/away bug fixed in abb4a16)"
    - "It writes history.parquet (home_team, away_team, home_goals, away_goals) and fixtures.parquet (fixture_id, home_team, away_team) in the exact schema build_lambda_store.py expects"
    - "Only fixtures whose final state is FINISHED (state.developer_name in FT/AET/FT_PEN/FINISHED) are included; in-progress/abandoned are skipped with a counted reason"
    - "The last snapshot per fixture (most complete) is used for the final score"
    - "Tests are OFFLINE and synthetic (no dependency on data/cache/ which is absent in worktrees) and assert: finished-only filter, last-snapshot selection, correct home/away mapping via the parser, and exact output schema"
    - "The producer is labelled a low-confidence bootstrap (model_version marker) and a # requires-real-data marker is present per project convention"
    - "engine_v3 suite + anti-Napoli regression pass before commit"
  artifacts:
    - path: "scripts/spike/v3/build_history_from_snapshots.py"
      provides: "snapshot -> history/fixtures parquet extractor feeding build_lambda_store.py"
  key_links:
    - from: "scripts/spike/v3/build_history_from_snapshots.py"
      to: "LiveMatchState.from_fixture (fixed parser)"
      via: "reuse of the regrade_day4_from_raw.py snapshot-parsing pattern"
      pattern: "from_fixture|model_validate|from_raw"
---

<objective>
Option 1 (validation bootstrap): turn the ~176 already-collected Sportmonks club-fixture
snapshots into the input `build_lambda_store.py` needs, so the ML-lambda pipeline can be
exercised end-to-end on REAL (but volume-thin, low-confidence) data — proving the wiring
(lambda store -> derive_priors_from_fixture -> _choose_dominant_team_id ML-lambda tier) works
without spending API quota. NOT a production model: ~176 matches across many leagues is too thin
for reliable Dixon-Coles ratings; this is structure-first validation, real per-league seasonal
corpus is a separate follow-up.

SCOPE: executor writes the extractor + OFFLINE synthetic tests ONLY. The actual run against the
real snapshots and the end-to-end wiring validation happen on `main` (orchestrator post-merge),
because data/cache/ does not exist inside agent worktrees.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@scripts/spike/v3/regrade_day4_from_raw.py
@scripts/spike/v3/build_lambda_store.py
</context>

<constraints>
- Reuse the EXISTING fixed parser path exactly like scripts/spike/v3/regrade_day4_from_raw.py
  does: read each snapshot JSON, Fixture.model_validate(record) -> LiveMatchState.from_fixture.
  Do NOT hand-parse scores/teams from raw JSON (the home/away bug abb4a16 lived precisely there).
- Match build_lambda_store.py's documented input schema EXACTLY:
  history-parquet: home_team:str, away_team:str, home_goals:int, away_goals:int
  fixtures-parquet: fixture_id:int, home_team:str, away_team:str
  (team-name vocabulary MUST be consistent between the two — use the same parser-derived names).
- FINISHED filter: include a fixture only if its final LiveMatchState is finished
  (state.developer_name in {FT, AET, FT_PEN, FINISHED}); count+log skipped (in-progress/abandoned/
  unparseable) without aborting the whole run.
- Use the LAST snapshot per fixture directory (lexicographic max of the ISO-named files) for the
  final score.
- All tests OFFLINE + synthetic (build tiny fake snapshot dicts / a tmp dir). data/cache/ is
  ABSENT in the worktree — do not read it in tests.
- Label low-confidence: the script must stamp/log a clear bootstrap marker and carry a
  `# requires-real-data` comment so the thin-corpus caveat is queued, per project convention
  (memory: structure-first, validate-with-data-later).
- ONE atomic commit. NO Co-Authored-By. Commit script + tests together. Do NOT commit
  PLAN/SUMMARY/STATE or any data/cache parquet.
- Run `uv run pytest tests/evaluation/live/engine_v3/ -q` + anti-Napoli before commit. The
  worktree venv may be incomplete — run `uv sync` first; report EXACT counts and verified cause
  of any failure (do NOT label failures pre-existing without checking the base commit).
</constraints>

<tasks>
<task id="1" type="execute">
files: scripts/spike/v3/build_history_from_snapshots.py (new),
tests/evaluation/live/engine_v3/test_build_history_from_snapshots.py (new)

action: Create `build_history_from_snapshots.py`. CLI:
`--snapshots-root data/cache/sportmonks/snapshots --out-history data/cache/lambda_history.parquet
--out-fixtures data/cache/lambda_fixtures.parquet`. For each `<fid>/` dir: load the last snapshot
JSON, extract the Sportmonks fixture record the same way regrade_day4_from_raw.py does, run it
through the fixed parser to a final LiveMatchState; if finished, emit a history row
(home_team, away_team, home_goals, away_goals) and a fixtures row
(fixture_id=int(fid), home_team, away_team) using the parser-derived team names (identical
vocabulary). Skip+count non-finished/unparseable. Write both parquets with Polars. Log a summary
(n_dirs, n_finished, n_skipped, reasons) and a prominent BOOTSTRAP / low-confidence /
requires-real-data banner. Add the OFFLINE synthetic test file: build a tmp snapshots tree with
2 finished + 1 in-progress + 1 unparseable fake fixtures; assert only the 2 finished appear, the
last-snapshot-per-dir is used, home/away mapping is correct via the parser, and both output
schemas match build_lambda_store.py's contract exactly.

verify: new tests pass; engine_v3 + anti-Napoli green.

done: commit `feat(scripts/v3): snapshot->history extractor for bootstrap lambda store (low-confidence, requires-real-data)`
with rationale: enables Option-1 end-to-end ML-lambda validation on ~176 already-collected
club fixtures without API quota; thin corpus = structure-first, real seasonal corpus is a
separate follow-up.
</task>
</tasks>

<orchestrator_post_merge_note>
After merge, on `main` (where data/cache/ exists) the ORCHESTRATOR runs:
1. build_history_from_snapshots.py against the real ~176 snapshots.
2. build_lambda_store.py on the produced history+fixtures -> data/cache/lambda_store.parquet.
3. End-to-end validation: confirm derive_priors_from_fixture reads the lambda store and the
   _choose_dominant_team_id ML-lambda tier activates on a decisive gap. Report n fixtures, n
   teams, cold-start rate, and an explicit low-confidence verdict. These artifacts are NOT
   committed (regenerable data/cache).
</orchestrator_post_merge_note>
